{
    "name": "selenium-mocha-sesi-9",
    "version": "1.0.0",
    "description": "Testing Saucedemo Using Selenium Mocha",
    "scripts": {
        "test": "mocha tests --recursive --timeout 60000",
        "test1": "mocha tests/test-login.js --recursive --timeout 60000",
        "test2": "mocha tests/test-urutanproduk.js --recursive --timeout 60000"
    },
    "dependencies": {
        "selenium-webdriver": "^4.17.0"
    },
    "devDependencies": {
        "mocha": "^10.4.0",
        "mocha-simple-html-reporter": "^2.0.0",
        "mochawesome": "^7.1.3"
    }
}